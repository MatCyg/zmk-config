#define SHIFTED(letter) \
    ZMK_MACRO(f_capital_##letter, \
        wait-ms = <0>; \
        tap-ms = <10>; \
        bindings = <&kp LS(letter) &tog SHORTCUT_F>; \
    ) \
    ZMK_MACRO(f_letter_##letter, \
        wait-ms = <0>; \
        tap-ms = <10>; \
        bindings = <&kp letter &tog SHORTCUT_F>; \
    ) \
    shifted_##letter: shifted_##letter { \
        compatible = "zmk,behavior-hold-tap"; \
        label = ZMK_MACRO_STRINGIFY(shifted_##letter); \
        #binding-cells = <2>; \
        tapping-term-ms = <180>; \
        flavor = "tap-preferred"; \
        bindings = <&f_capital_##letter>, <&f_letter_##letter>; \
    };


SHIFTED(A)
SHIFTED(B)
SHIFTED(C)
SHIFTED(D)
SHIFTED(E)
SHIFTED(F)
SHIFTED(G)
SHIFTED(H)
SHIFTED(I)
SHIFTED(J)
SHIFTED(K)
SHIFTED(L)
SHIFTED(M)
SHIFTED(N)
SHIFTED(O)
SHIFTED(P)
SHIFTED(Q)
SHIFTED(R)
SHIFTED(S)
SHIFTED(T)
SHIFTED(U)
SHIFTED(V)
SHIFTED(W)
SHIFTED(X)
SHIFTED(Y)
SHIFTED(Z)
SHIFTED(SCLN)
SHIFTED(CMMA)
SHIFTED(DOT)
SHIFTED(FSLH)
